<div>
  <div>
    <h1>
      List of all photos
    </h1>
  </div>
</div>

<%= params%>


<table border="1">
  <tr>
    <th>Username</th>

    <th>Private? </th>

    <th></th>

    <th></th>
    
  </tr>


<%=matching_follow_requests = FollowRequest.where({ :sender_id => current_user.id })%>


<% @list_of_users.each do |a_user|%>
  <tr>
    <td><%= a_user.username%></td>

    <td> <%= a_user.private%></td>

    
    <!--<td><%#= @follow_requests.where(:sender_id == a_user.id)%></td>-->

    <td> <a href="/users/<%= a_user.username%>">Show details</a> </td>

  </tr>
<%end%>
</table>
